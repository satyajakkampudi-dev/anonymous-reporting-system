# App Architecture Diagram — Anonymous Reporting System

> LoG.ai Layer 1. Two micro-apps (each its own bot, mobile + web), one shared data cluster, and a
> shared `/lib`. Cross-app arrows are bot-to-bot `MSG_*` contracts — **all reporter-identity-free**.
> Source: [`../REQUIREMENTS.md`](../REQUIREMENTS.md) §4/§11, decisions D1–D17.

```mermaid
graph TB
    subgraph Actors
        R["👤 Reporter<br/>crew (any vessel) + shore staff<br/>mobile + web"]
        P["👤 Primary admin<br/>central shore compliance<br/>mobile + web"]
        S["👤 Secondary admin<br/>senior / escalation (DPA)<br/>mobile + web"]
    end

    subgraph Apps["frontm.ai micro-apps (own bot each)"]
        U["📱 anonymous-user<br/>submit · track · accept/reject ·<br/>withdraw · anonymous call<br/>(own reports only)"]
        A["🛡️ anonymous-admin<br/>queue · manage · transitions ·<br/>manual log · jobs · analytics · calls<br/>(role-gated, identity-stripped)"]
    end

    LIB["📦 /lib (shared, single source of truth)<br/>constants · ticket-status · id-generator ·<br/>validation · notifications · access (adminProjection +<br/>resolveAssignees D17) · calling · utils (format/platform/theme)"]
    JOB["⏱️ state.jobScheduler<br/>auto-escalate · auto-close · SLA digest"]

    subgraph Data["Shared MongoDB (shared: true, *_${systemId})"]
        DBR[("reports<br/>audit: true<br/>admin reads via adminProjection")]
        DBC[("call-queue<br/>identity-free")]
        DBA[("admin-users<br/>role · scope=GLOBAL · availability")]
    end

    %% actor -> app
    R -->|uses| U
    P -->|uses| A
    S -->|uses| A

    %% apps -> shared lib (side-effect import of collections + helpers)
    U -.->|imports| LIB
    A -.->|imports| LIB

    %% data access
    U -->|"write reports (own); create call entries"| DBR
    U -->|"RINGING entry; read available admins"| DBC
    A -->|"read (adminProjection) + transitions (audited)"| DBR
    A -->|"answer/claim; lifecycle states"| DBC
    A -->|"read role+availability; admins set own availability"| DBA
    U -.->|"read available admins to ring"| DBA

    %% jobs run inside admin app
    A -->|schedules / fires| JOB
    JOB -->|fires intents| A

    %% cross-app bot-to-bot contracts (identity-free)
    U ==>|"MSG_NEW_REPORT"| A
    U ==>|"MSG_REPORT_REOPENED"| A
    U ==>|"MSG_INCOMING_CALL (+VoIP push)"| A
    A ==>|"MSG_REPORT_RESOLVED"| U
    A ==>|"MSG_REPORT_STATUS_CHANGED"| U
    A ==>|"MSG_REPORT_CLOSED"| U
    A ==>|"MSG_CALL_STOP_RING (admin↔admin)"| A

    classDef app fill:#e3f2fd,stroke:#1565c0,stroke-width:2px;
    classDef data fill:#fff3e0,stroke:#e65100;
    classDef lib fill:#e8f5e9,stroke:#2e7d32;
    classDef job fill:#fce4ec,stroke:#ad1457;
    class U,A app;
    class DBR,DBC,DBA data;
    class LIB lib;
    class JOB job;
```

## Notes

- **Role-based visibility, not a third app.** Primary and Secondary admin share `anonymous-admin`;
  the queue is role-filtered (Primary: OPEN/UNDER_REVIEW; Secondary: ESCALATED + against-admin).
- **Anonymity is a code chokepoint, not a topology.** Every admin read flows through
  `loadReportsForAdmin()` / `loadReportForAdmin()` applying `adminProjection` (ER-A3); routing flows
  through `resolveAssignees(report)` (D17, `scope = GLOBAL` in v1). No admin path queries `reports`
  directly; no bot-to-bot payload or email carries `reporterId`/contact.
- **Future fleet/region scoping is additive** — populate `admin-users.scope`, add a structured
  vessel→scope map, extend `resolveAssignees`. No new app, no report-schema change.
