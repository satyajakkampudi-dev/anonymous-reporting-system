# Field Specification — Anonymous Reporting System

> LoG.ai Layer 3 (the Director's Notes). Field-level precision for each micro-app, derived from
> the BRD ([`2.brd.md`](2.brd.md)), the data-model spec ([`SPEC.md`](SPEC.md)), and the decisions
> D1–D17 in [`../REQUIREMENTS.md`](../REQUIREMENTS.md) §15. British English; frontm.ai is always
> lowercase. **A section's `rendering:` is mandatory** (`standard` / `custom_card` / `display_only`).
>
> Companion artefacts: [`3.anonymous-user-input-schema.md`](3.anonymous-user-input-schema.md),
> [`3.framework-mapping.md`](3.framework-mapping.md), [`3.wireframes-anonymous-user.md`](3.wireframes-anonymous-user.md).
>
> **Pass status:** anonymous-user is complete. **anonymous-admin to follow** (PM chose user-app-first sequencing).

---

## Shared collections (defined once in `lib/collections/`, side-effect-imported by both apps)

| Collection | MongoDB name | shared | system | nameOverride | PK | Notes |
|---|---|:--:|:--:|:--:|---|---|
| `reports` | `reports_${systemId}` | true | false | false | `reportId` | `audit: true`. The primary entity. |
| `call-queue` | `call_queue_${systemId}` | true | false | false | `callRef` | Identity-free call attempts. |
| `admin-users` | `admin_users_${systemId}` | true | false | false | `adminUserId` | Seeded registry (D3). Admin app owns writes. |

All three are `shared: true` → suffix `systemId` (data survives across conversations/bots — required
because both apps and all conversations read the same reports). None use `nameOverride`.

---

# anonymous-user

## Entity Definition — Report (reporter's view)

```
Entity:       Report
Collection:   reports   (MongoDB reports_${systemId})
Shared:       true
System:       false
NameOverride: false
Architecture: Standalone Collection
              · My Reports list → reportsCollection.loadCollectionWithQuery({ query: { reporterId } })
              · Submit / Detail  → reportDoc.loadDocument({ reportId })  (ownership asserted: reporterId === state.user.userId)
PrimaryKey:   reportId   (RPT- + 10 chars, generated via lib/id-generator.js on first save; retry on unique-index collision — ER-B9)
UserFilter:   reporterId (= state.user.userId; reporter-private, hidden, NOT the primaryKey)
Layout:       Four screens, separate intents (NOT tabs):
              Home/landing (U-F0) · Submit (U-F3/4) · My Reports (U-F1) · Report detail (U-F2)
              + Amend popup (U-F13) · Call compliance (U-F15/16/17)
Architecture decision: TWO-DOC (see §1.5 below)
  Data Doc:    reportDoc          — all fields + sub-collections + persistence. Submit form renders this (standard sections). Never sent for display screens.
  Display Doc: reportDisplayDoc   — CardsSet sections for Home, My Reports list, Detail. sendResponse() renders this.
```

### Infrastructure fields (derived — NOT PM-specified, NON-NEGOTIABLE)

These never appear in a wireframe; they are framework requirements of the load/query pattern.

| Field | Type | Properties | Source / purpose |
|---|---|---|---|
| `reportId` | text | `primaryKey: true`, `hidden: true` | Generated `state.getUniqueId()`-style via `lib/id-generator.js` (`RPT-`+10). Unique per row. Without it, `save()` upserts `{}` and the collection dedupes all rows to one. |
| `reporterId` | text | `hidden: true`, `dbName: "reporterId"`, NOT primaryKey | `state.user.userId`. Sets the `loadCollectionWithQuery` filter and the ownership assertion. Reporter-private — excluded from `adminProjection`. |
| `status` | text | `hidden: true` (on submit) | Set `OPEN` at submit (U-F8). Read-only displayed on detail. |
| `severity` | text | `hidden: true` | Derived from `urgency` at submit (D6); admin may override. Not editable by reporter. |
| `source` | text | `hidden: true` | `REPORTER` (submit) / `CALL` (voicemail) / `MANUAL` (admin). |
| `assignedTo` | text | `hidden: true` | `resolveAssignees(report)` at submit (D17). |
| `createdOn` / `updatedOn` | number | `hidden: true` | ms timestamps. `updatedOn` backs optimistic-concurrency (ER-B5). |
| `version` | number | `hidden: true` | Incremented per write; transition guard. |
| `reopenCount` | number | `hidden: true` | Default 0; 0→1 once (D10). |
| `withdrawnOn` / `resolvedOn` | number | `hidden: true` | Set on the respective terminal/resolved transitions. |

> **autoSave bootstrap (CRITICAL).** `reportDoc` is `autoSave: true` (U-F9 draft autosave). Therefore
> `src/main.js` MUST set `state.onConfig = () => { state.contextAware = true; }` AND `main.onResolution`
> MUST call `await Context.CreateAndInit("mainApp", { state })` before any `loadDocument`. See BRD §8.1
> and CLAUDE.md "App Entry-Point Bootstrap". Subsequent popup intents (Amend) use `Context.Create`, never re-`loadDocument`.

## §1.5 Rendering Architecture Decision — anonymous-user

**Two-Doc.** §9 of REQUIREMENTS specifies custom HTML cards ("sailors-cart style") for Home, My
Reports, and Detail; the Submit form is a standard Form Doc. Any `custom_card`/`display_only`
section forces Two-Doc.

```
Architecture: Two-Doc
Data Doc:     reportDoc           (always present — owns every Field + both sub-collections)
Display Doc:  reportDisplayDoc    (Home landing card, My Reports list card, Detail cards)
Sections needing Display Doc: Home landing, My Reports list, Report header, Reporter content (detail),
                              Status timeline, Resolution, Amendment list (detail view)
```

Per §9.1, every Display section dispatches once via `renderForPlatform(data, { web, mobile })` —
distinct `web.js`/`mobile.js` renderers, shared theme tokens (`lib/utils/theme.js`).

---

## Section: Home / landing (U-F0) — rendering: `display_only`

Entry screen. Anonymity reassurance + three action buttons. No persisted fields (purely navigational).

**Display elements (HTML card, per-platform):**

| Element | Source | Format | Notes |
|---|---|---|---|
| Trust banner | static copy (`lib/utils/format.js` builder) | text | "Your identity is never shown to the compliance team." (ER-D16). |
| Anonymity intro | static copy | text | One paragraph: what the system does + that it is code-enforced anonymous. |
| Action: Submit a report | button → intent `openSubmitReport` | button | `data-action="intent"`, `data-intent-id="openSubmitReport"`. Primary CTA. |
| Action: My Reports | button → intent `openMyReports` | button | `data-action="intent"`. |
| Action: Call compliance (anonymous) | button → intent `startAnonymousCall` (U-F15) | button | `data-action="intent"`. Secondary/voice CTA. |

No `fields:` array (display-only, no persistence).

---

## Section: Report details (Submit form) — rendering: `standard`

The submission form (U-F3/F4). Standard Form Doc fields on `reportDoc`. `columns: 2` where noted;
fields alternate `column: 0,1` in declaration order. Draft-autosaved (U-F9).

| Field | Type | Mandatory | Editable | Column | Notes |
|---|---|:--:|:--:|:--:|---|
| `category` | dropdown | ✅ | ✅ | 0 | Options: Harassment/abuse · Safety violation · Fraud/ethics breach · Bullying/retaliation · Other. Fixed list. |
| `urgency` | dropdown | ✅ | ✅ | 1 | Options: Immediate risk · High · Medium · Low. Drives initial `severity` (D6). |
| `shipName` | text | ❌ | ✅ | 0 | Free text (incident metadata, NOT a routing key — D17). Sanitised. |
| `location` | dropdown | ✅ | ✅ | 1 | Options: Onboard vessel · Office/shore base · Remote/digital · Other. |
| `incidentDate` | date | ✅ | ✅ | 0 | DATE picker; validated parseable + not in the future (U-F8). |
| `againstAdmin` | switch | ❌ | ✅ | 1 | If on → routes `assignedTo = SECONDARY_ADMIN` (D9). Label: "This concerns a member of the compliance team." |
| `description` | textarea | ✅ | ✅ | full | Sanitised before any HTML/email use. Multi-line. |
| `accusedParty` | text | ❌ | ✅ | full | Optional. Sanitised. Hint: avoid self-identifying detail (ER-A1). |

**Field-type semantic review (2c):** `incidentDate`→DATE (picker, not text). `againstAdmin`→SWITCH
(toggle, not Yes/No dropdown). `category`/`urgency`/`location`→DROPDOWN (fixed <20-item lists, not
free text). `description`→TEXT_AREA (multi-line, not single-line TEXT). All confirmed.

---

## Section: Contact (Submit form) — rendering: `standard`

Reporter-private contact channel (U-F7). Conditional validation per method. **Reporter-private —
excluded from `adminProjection`; `contactValue` is `encrypted: true`.**

| Field | Type | Mandatory | Editable | Column | Notes |
|---|---|:--:|:--:|:--:|---|
| `contactMethod` | dropdown | ✅ | ✅ | 0 | Options: None · Email · Phone · Cabin number. Default None. |
| `contactValue` | text | conditional | ✅ | 1 | `encrypted: true`. Hidden/cleared when method = None. Validated per method (U-F7): Email→RFC-pragmatic regex; Phone→E.164-tolerant; Cabin→alphanumeric. Mandatory only when method ≠ None. |

> **Conditional reveal.** `contactValue` is shown/required only when `contactMethod ≠ None`. The
> framework has no declarative "show-if"; this is enforced in `contactValue.onValidation` +
> `contactMethod.onSave` (validation by current method) — a custom frame (U-F7).
> **Field-type note:** `contactValue` stays a single TEXT field (not EMAIL/PHONE) because its meaning
> changes with `contactMethod`; the per-method validator lives in `lib/validation.js`.

---

## Section: Evidence (Submit form) — rendering: `standard`

Up to 5 files, ≤ 25 MB each (D1). Validate extension **and** content type server-side (U-F6, ER-C10).

| Field | Type | Mandatory | Editable | Notes |
|---|---|:--:|:--:|---|
| `evidenceFile1` | file | ❌ | ✅ | FILE_FIELD. Allowed: images, PDF, doc/docx, audio, video, text. ≤ 25 MB. |
| `evidenceFile2` | file | ❌ | ✅ | As above. |
| `evidenceFile3` | file | ❌ | ✅ | As above. |
| `evidenceFile4` | file | ❌ | ✅ | As above. |
| `evidenceFile5` | file | ❌ | ✅ | As above. |
| `evidenceNotes` | textarea | ❌ | ✅ | Optional. Sanitised. |

**Field-type semantic review (2c):** evidence → FILE_FIELD (upload control + S3), **never** TEXT
with a pasted URL. The `.value` is an envelope `{ value: <s3-key>, fileName, fileScopeValue }`;
admin-side display requires a signed URL (A-F7) — see anonymous-admin pass. Validation (type+size)
is server-side in `lib/validation.js`, called from the submit `onSubmit` (U-F6), atomic with save (ER-C10).

---

## Sub-collection: Amendments (U-F13) — rendering: `custom_card`, append-only

Append-only trail of post-submission notes. `forCollection: true` on `reportDoc`. **No edit/delete**
— add only. Visible in the detail screen (display) and added via a `sendQuickFormResponse()` popup.

**Table columns (detail display, read-only list):**

| Header | Source | Format | Notes |
|---|---|---|---|
| When | `amendedOn` | date | Formatted via `lib/utils/format.js`. |
| Note | `amendmentNote` | text | Sanitised render. |
| Evidence | `amendmentEvidenceKey` | file-link | Signed-URL link if present, else "—". |

**Fields (popup form — `add_intent: addAmendment`):**

| Sub-collection field | Type | Mandatory | Editable | Notes |
|---|---|:--:|:--:|---|
| `amendmentId` | text | — | — | `primaryKey: true`, `hidden: true`. Generated per entry. |
| `amendmentNote` | textarea | ✅ | ✅ | `includeInQuickEdit: true`. Sanitised. |
| `amendmentEvidenceKey` | file | ❌ | ✅ | `includeInQuickEdit: true`. Optional single evidence file (same validation as submit evidence). |
| `amendedOn` | number | — | — | `hidden: true`. Set to now on submit. |

> Only on **non-terminal** reports (BRD U-F13). The "Amend" button is gated on
> `status.terminal === false` (from `lib/ticket-status.js` meta). Original reporter-entered fields
> stay locked — amendments never rewrite history.

---

## Sub-collection: Status history (timeline) — rendering: `display_only`, read-only

The status timeline (U-F2). `forCollection: true` on `reportDoc`; **written by transitions**, never
by the reporter. Pure display on the user side.

**Display elements (timeline HTML, detail screen):**

| Element | Source | Format | Notes |
|---|---|---|---|
| Timeline rows | `statusHistory` rows | timeline | One node per transition: status label + tone (from `lib/ticket-status.js` meta), `changedOn` date, optional `note`. Newest first or oldest first per platform renderer. |

**Fields (persistence — written by the transition path, not a popup):**

| Sub-collection field | Type | Properties | Notes |
|---|---|---|---|
| `historyId` | text | `primaryKey: true`, `hidden: true` | Generated per entry. |
| `fromStatus` / `toStatus` | text | hidden (data) | STATUS values; rendered via status meta. |
| `actorRole` | text | hidden (data) | `REPORTER / PRIMARY_ADMIN / SECONDARY_ADMIN / SYSTEM` — **role only, never an id** (anonymity). |
| `changedOn` | number | hidden (data) | ms timestamp. |
| `note` | textarea | hidden (data) | Optional, sanitised (e.g. reject reason on REOPENED). |

No `add/edit/delete` intents on the user side — display only.

---

## Screen: My Reports (U-F1) — rendering: `custom_card` (collection list)

Card list of the reporter's **own** reports only (`reporterId === state.user.userId`). Search/filter
by status and category. Loaded via `reportsCollection.loadCollectionWithQuery({ query: { reporterId } })`
before `sendResponse()`.

**Card per report (read-only list, per-platform):**

| Element | Source | Format | Notes |
|---|---|---|---|
| Tracking ID | `reportId` | text | Prominent (e.g. `RPT-AB12CD34XY`). |
| Status pill | `status` | status-pill | Label + tone from `lib/ticket-status.js` meta. |
| Category | `category` | text | |
| Urgency | `urgency` | text | |
| Date | `createdOn` | date | Submitted date. |
| Open → | row → intent `openReportDetail` | button | `data-action="intent"`, `data-payload='{"reportId":"<id>"}'`. |

**Filters:** quick-filter chips by `status` group (Open / In progress / Resolved / Closed) and by
`category`; text search over `reportId`. Empty state: "You have not submitted any reports yet" + a
Submit CTA.

---

## Screen: Report detail (U-F2) + actions — rendering: `custom_card` / `display_only`

Loaded via `reportDoc.loadDocument({ reportId })`; **ownership asserted** (`reporterId ===
state.user.userId`) before any render — else a clear "not found / not yours" message. Composed of
several Display Doc cards.

**Display cards:**

| Card | Source | Format | Notes |
|---|---|---|---|
| Header | `reportId`, `status`, `severity`, `category`, `urgency`, `createdOn` | header card | Status pill + tone; tracking ID prominent. |
| Reporter content | `shipName`, `location`, `incidentDate`, `description`, `accusedParty`, evidence files | content card | Read-only echo of what was submitted. Evidence as signed-URL download links ("download at your own risk" not needed reporter-side; signed URLs still required for private S3). |
| Status timeline | `statusHistory` | timeline | See sub-collection above. |
| Resolution | `resolution`, `resolvedOn` | resolution card | Shown only when a resolution exists (`status` RESOLVED/REOPENED/CLOSED_*). |
| Actions | derived from `status` meta `allowedActionsByRole` (reporter) | button row | See below. |

**Action buttons (gated by `lib/ticket-status.js` meta for the reporter role):**

| Button | Visible when | Intent | Frame | Effect |
|---|---|---|---|---|
| Amend | `status` non-terminal | `addAmendment` | U-F13 | Append-only popup (sub-collection above). |
| Withdraw | `status` ∈ {OPEN, UNDER_REVIEW} | `withdrawReport` | U-F12 | → WITHDRAWN (terminal). Confirm dialog. |
| Accept | `status` = RESOLVED | `acceptResolution` | U-F10 | → CLOSED_BY_USER (terminal). |
| Reject + reason | `status` = RESOLVED AND `reopenCount` < 1 | `rejectResolution` | U-F11 | Reason popup → REOPENED, `reopenCount` 0→1; sends `MSG_REPORT_REOPENED`. After the cap the button is hidden (D10). |

Every action re-reads the report and validates the transition against current `status`+`version`
(ER-B5) before writing; stale writes are surfaced, not overwritten.

---

## Screen: Call compliance (anonymous) (U-F15/16/17) — action + voicemail

Not a Doc form — an action button (Home or its own screen) that creates a masked voice-only meeting.

| Step | Frame | Behaviour | call-queue fields written |
|---|---|---|---|
| Tap "Call compliance" | U-F15 | Create voice-only Daily.co meeting, masked/system host (never `state.user.userEmail`); reporter joins as throwaway masked guest "Anonymous Reporter", video muted. Create `RINGING` entry. Ring `available` admins via `MSG_INCOMING_CALL` (callRef + meetingId only). | `callRef`, `status=RINGING`, `meetingId`, `createdOn` |
| 30 s no answer (D7) | U-F16 | → `MISSED`; prompt voicemail (≤ 3 min / 25 MB → domain S3); auto-create report (`source=CALL`, voicemail as evidence, `reporterId` empty, `OPEN`); `MSG_NEW_REPORT`. | `status=MISSED`, `endedOn`, `voicemailKey`, `linkedReportId` |
| Hang-up before answer / mid-call drop | U-F17 | `RINGING→ABANDONED`; mid-call inactivity `ACTIVE→ENDED`. | `status`, `endedOn`, `durationMs` |

`call-queue` is identity-free (see SPEC.md). No recording (ER-A5). Full field table for `call-queue`
is shared and detailed in the anonymous-admin pass (admin side answers calls).

---

## Lookup dependencies — anonymous-user

| Lookup collection | Referenced by | Already exists? | Display field | Notes |
|---|---|:--:|---|---|
| — | — | — | — | **None.** All choice fields are fixed DROPDOWNs (category/urgency/location/contactMethod). `assignedTo` routing uses `resolveAssignees` over `admin-users` in code (not a UI lookup). No reporter-facing lookup field. |

---

## Custom frames — anonymous-user (enriched from BRD §7)

> Standard CRUD scaffolding (open screens, render lists) is inferred. Below are the PM-confirmed
> custom frames, now carrying field names. Reporter notification dispatch (U-F14) is a side-effect
> of the action frames + inbound contracts.

| Frame | Intent | Trigger | Reads | Writes | Rule |
|---|---|---|---|---|---|
| U-F5 Pre-submit anonymity guard | (submit form render + pre-submit) | form open / submit | admin-visible field set | none | Warning + "what the admin will see" preview from `adminProjection` set (ER-A1). |
| U-F6 Evidence validation + atomicity | `reportDoc.onSubmit` (+ field onSave) | file attach / save | `evidenceFile1..5` | — | ≤5 files/≤25 MB; type+content validation server-side; no orphaned S3 / no saved-without-evidence (ER-C10). |
| U-F7 Contact conditional validation | `contactValue.onValidation`, `contactMethod.onSave` | method select / save | `contactMethod`, `contactValue` | `contactValue` | None hides value; Email/Phone/Cabin validators (FR-U1). |
| U-F8 Submit transforms + idempotency | `reportDoc.onSubmit` | submit | all free-text | `reportId`, `status`, `assignedTo`, `severity`, `createdOn`, `source`, `version` | Sanitise; `incidentDate` not future; generate `reportId` + retry on collision (ER-B9); `status=OPEN`; `severity` from `urgency` (D6); route via `resolveAssignees` (D17); debounce double-submit. Append first `statusHistory` row (→OPEN). |
| U-F9 Draft autosave | `reportDoc` `autoSave: true` | field edits | all submission fields | autoSaveBuffer | Survives crash/navigation (D14). Requires Context bootstrap. |
| U-F10 Accept | `acceptResolution.onResolution` | button | report by `reportId` | `status`, `version`, `updatedOn` | RESOLVED→CLOSED_BY_USER. Ownership + concurrency guard. Appends `statusHistory`. |
| U-F11 Reject | `rejectResolution.onResolution` | button + reason popup | report by `reportId` | `status`, `reopenCount`, `rejectReason`, `version` | RESOLVED→REOPENED, 0→1 once (D10); sends `MSG_REPORT_REOPENED`. Appends `statusHistory` (note = reason). |
| U-F12 Withdraw | `withdrawReport.onResolution` | button | report by `reportId` | `status`, `withdrawnOn`, `version` | {OPEN,UNDER_REVIEW}→WITHDRAWN. Appends `statusHistory`. |
| U-F13 Amend (append) | `addAmendment.onResolution` → popup | button | report by `reportId` | `amendments` row | Append-only timestamped note + optional evidence; original fields locked (D16). |
| U-F14 Reporter notification dispatch | (within action frames + inbound onMatching) | own action / inbound MSG | — | — | Email + web push on received/status-changed/resolved/closed (best-effort, logged). |
| U-F15/16/17 Anonymous calling | `startAnonymousCall` (+ timeout/hangup handlers) | button / 30s / hang-up | admin-users (available) | `call-queue` row | Masked voice-only meeting; ring; voicemail→auto-create report. See SPEC.md calling model. |

### Custom-card popup/action intents (frame IDs)

```
Frame ID:    U-F13  (addAmendment)
Trigger:     "Amend" button (data-action="intent") in Report detail card
Target:      amendments sub-collection (forCollection on reportDoc)
Mechanism:   sendQuickFormResponse  (reportDoc amendment sub-entity, confirm:"Save" cancel:"Cancel")
Payload:     { reportId }   (read from state.messageFromUser.payload.reportId)
Description: Open an append-only amendment form; on save append the row, persist via
             self.collection.parentDoc.save(), re-render reportDisplayDoc.

Frame ID:    openReportDetail / openMyReports / openSubmitReport / startAnonymousCall
Trigger:     action buttons (data-action="intent")
Mechanism:   navigate (load + sendResponse of reportDisplayDoc)
Payload:     { reportId } for openReportDetail (else none)
```

## Cross-app contracts — anonymous-user side

```yaml
# SENDS
- id: U-X1
  message_type: MSG_NEW_REPORT
  to_bot: anonymous-admin
  trigger: after reportDoc.save() in U-F8 (submit) and U-F16 (voicemail auto-create)
  payload: [reportId, category, urgency, severity, assignedTo, createdOn]   # identity-free
- id: U-X2
  message_type: MSG_REPORT_REOPENED
  to_bot: anonymous-admin
  trigger: after U-F11 reject persists
  payload: [reportId, reopenCount, rejectReason]
- id: U-X3
  message_type: MSG_INCOMING_CALL
  to_bot: anonymous-admin
  trigger: U-F15 after RINGING entry created
  payload: [callRef, meetingId]   # + VoIP push

# RECEIVES (onMatching, Context B — load report by reportId before reading)
- message_type: MSG_REPORT_RESOLVED      # from anonymous-admin → notify reporter (U-X4/U-F14)
- message_type: MSG_REPORT_STATUS_CHANGED # (U-X5/U-F14)
- message_type: MSG_REPORT_CLOSED         # (U-X6/U-F14)
```

> **Send after save** (never before persistence). **Minimal payload** — IDs + display text only;
> the receiver loads full data from the shared `reports` collection through `adminProjection`.
> Incoming messages run in **sandbox** — persist any state via `setPersistedField`.

---

# anonymous-admin

> **To follow** in the next pass (PM chose user-app-first). Will cover: Dashboard (A-F2, custom HTML
> stat cards + small-cell suppression), Report queue (A-F3/F4/F5, role-filter + recusal + priority
> surfacing), Report manage (A-F6/F7/F8–F12, signed-URL evidence + transitions + severity override),
> Manual log (A-F13), Case export (A-F14), Alerts/digest (A-F19), On-call + calling (A-F20/F21/F22),
> system jobs (A-F16/F17/F18), the `admin-users` and `call-queue` full field tables, and the
> `adminProjection` binding (no identity field bound on any admin Doc).
