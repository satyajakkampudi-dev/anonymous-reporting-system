// Collision-resistant report-ID generation (prefix "RPT-").
// See ../specs/SPEC.md. Deduped from the two legacy repos into one module.
// NOTE: skeleton placeholder — implemented during the foundation (B1) build.

// Generates a new unique report ID, e.g. "RPT-AB12CD34XY".
export const generateReportId = () => "";
