// Shared constants — single source of truth for both microapps.
// See ../REQUIREMENTS.md §5 and ../specs/SPEC.md for the canonical values.
// NOTE: skeleton placeholders — values are filled in during the foundation (B1) build.

// Report categories (reporter-selected).
export const CATEGORY = {};

// Reporter-selected urgency.
export const URGENCY = {};

// Admin/system-assessed severity.
export const SEVERITY = {};

// Incident location.
export const LOCATION = {};

// Optional reporter contact channel.
export const CONTACT_METHOD = {};

// Admin roles + routing targets.
export const ROLE = {};

// Report origin.
export const SOURCE = {};

// Cross-app (bot-to-bot) message types. Payloads are always identity-free.
export const MSG = {};
