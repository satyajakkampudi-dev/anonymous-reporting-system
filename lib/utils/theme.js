// Shared theme tokens — single source of truth for the visual identity used by
// BOTH the web and mobile renderers, so the look stays consistent while layouts
// differ per platform (see ../../REQUIREMENTS.md §9.1).
// Colours, spacing, typography, and per-status tones live here only — never
// hardcoded inside individual screen renderers.
// NOTE: skeleton placeholders — filled in during the core-apps (B2) build.

// Brand + surface colours.
export const COLORS = {};

// Spacing scale (e.g. xs/sm/md/lg in px).
export const SPACING = {};

// Typography (font family, sizes, weights).
export const TYPOGRAPHY = {};

// Visual tone per report status (background/text/badge), keyed by STATUS.
export const STATUS_TONES = {};
