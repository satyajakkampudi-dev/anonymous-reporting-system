// Custom-UI helpers shared by both apps: HTML escaping + card/HTML builders for
// the Display-Doc + CardsSet rendering pattern (../REQUIREMENTS.md §9).
// NOTE: skeleton placeholders — implemented during the foundation (B1) build.

// Escapes a string for safe inclusion in HTML card content.
export const escapeHtml = (value) => value;

// Builds the HTML for a single report summary card (used in list views).
export const reportCardHtml = () => "";

// Builds the HTML for a status timeline (used in report detail views).
export const statusTimelineHtml = () => "";
